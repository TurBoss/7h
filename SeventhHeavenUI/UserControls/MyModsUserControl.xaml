<UserControl x:Class="SeventhHeaven.UserControls.MyModsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SeventhHeaven.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

        <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource MainListViewItemStyle}">
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </Style>

    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <ListView x:Name="lstMods" ItemsSource="{Binding ModList}"  SelectionChanged="lstMods_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="250" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Author" Width="100" DisplayMemberBinding="{Binding Author}"/>
                    <GridViewColumn Header="Version" Width="100" DisplayMemberBinding="{Binding Version}"/>
                    <GridViewColumn Header="Category" Width="100" DisplayMemberBinding="{Binding Category}"/>
                    <GridViewColumn Width="100" DisplayMemberBinding="{Binding Empty}"/>
                    <GridViewColumn x:Name="colActive" Header="Active" Width="75">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <CheckBox.Style>
                                        <Style TargetType="{x:Type CheckBox}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type CheckBox}">
                                                        <Grid HorizontalAlignment="Center" Width="{Binding ElementName=colActive, Path=ActualWidth}">
                                                            <Viewbox Stretch="UniformToFill" Width="32">
                                                                <Canvas Background="Transparent" x:Name="canvas" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Width="76" Height="76" Clip="F1 M 0,0L 76,0L 76,76L 0,76L 0,0">
                                                                    <Path x:Name="iconCircle" Width="42.75" Height="42.75" Canvas.Left="16.625" Canvas.Top="16.625" Stretch="Fill" Fill="Red" Data="F1 M 38,27.1542C 43.99,27.1542 48.8458,32.01 48.8458,38C 48.8458,43.99 43.99,48.8458 38,48.8458C 32.01,48.8458 27.1542,43.99 27.1542,38C 27.1542,32.01 32.01,27.1542 38,27.1542 Z M 38,16.625C 49.8051,16.625 59.375,26.1949 59.375,38C 59.375,49.8051 49.8051,59.375 38,59.375C 26.1949,59.375 16.625,49.8051 16.625,38C 16.625,26.1949 26.1949,16.625 38,16.625 Z M 38,20.5833C 28.381,20.5833 20.5833,28.381 20.5833,38C 20.5833,47.619 28.381,55.4167 38,55.4167C 47.6189,55.4167 55.4167,47.619 55.4167,38C 55.4167,28.381 47.619,20.5833 38,20.5833 Z "/>
                                                                </Canvas>
                                                            </Viewbox>
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                                <Setter TargetName="iconCircle" Property="Fill" Value="Green"/>
                                                            </DataTrigger>
                                                            <Trigger SourceName="canvas" Property="IsMouseOver" Value="True">
                                                                <Setter Property="Cursor" Value="Hand"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </CheckBox.Style>
                                </CheckBox>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
            
        </ListView>

        <StackPanel Grid.Column="1"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Right">

            <Button x:Name="btnImport" 
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconImport}"
                    ToolTip="Import Mod"
                    Margin="0,0,0,0"/>

            <Button x:Name="btnRefresh" 
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconRefresh}"
                    Click="btnRefresh_Click"
                    ToolTip="Refresh List"
                    Margin="0,0,0,0"/>

            <Button x:Name="btnDeactivateAll" 
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconCancel}"
                    Click="btnDeactivateAll_Click"
                    ToolTip="Deactivate All"
                    Margin="0,0,0,0"/>

            <Button x:Name="btnUninstall" 
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconDelete}"
                    Click="btnUninstall_Click"
                    ToolTip="Uninstall Mod"
                    Margin="0,0,0,0"/>

            <Button x:Name="btnConfigure" 
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconSettings}"
                    ToolTip="Configure Mod"
                    Margin="0,0,0,10"/>

            <Button x:Name="btnMoveTop" 
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconUpArrow}"
                    Click="btnMoveTop_Click"
                    ToolTip="Move to Top"
                    Margin="0,0,0,0"/>

            <Button x:Name="btnMoveUp"
                    Click="btnMoveUp_Click"
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconUpChevron}"
                    ToolTip="Move Up"
                    Margin="0,0,0,0"/>
            
            <Button x:Name="btnMoveDown"
                    Click="btnMoveDown_Click"
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconDownChevron}"
                    ToolTip="Move Down"/>

            <Button x:Name="btnSendBottom" 
                    Style="{StaticResource ImageButton}"
                    Content="{StaticResource iconDownArrow}"
                    Click="btnSendBottom_Click"
                    ToolTip="Send to Bottom"
                    Margin="0,0,0,0"/>
        </StackPanel>
        
    </Grid>
</UserControl>
